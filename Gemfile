source 'https://rubygems.org'

# Use github-pages gem if you want to match GitHub Pages environment
gem 'github-pages', group: :jekyll_plugins

# If you use only Jekyll (not the full github-pages gem), use:
# gem 'jekyll', '~> 4.3.2'
